# 🏗️ ARCHITECTURE DIAGRAM  

```
┌──────────────────────────────────────────────────────────────────────┐
│                                                                      │
│  USER: "I have $20,000 debt at 2.5% interest. Create a payoff plan" │
│                                                                      │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │ MAIN ASSISTANT  │ ← Loads memories from store
                    │   (Pass 1)      │ ← Formats system prompt
                    └────────┬────────┘ ← Calls LLM with RouteIntent tool
                             │
                             │ Tool Call: RouteIntent(intent="debt_payoff_plan")
                             ▼
                    ┌─────────────────┐
                    │     ROUTER      │ ← Reads intent from tool call
                    └────────┬────────┘ ← Looks up executor in registry
                             │
                             │ Route to: finance_debt_payoff_executor
                             ▼
                    ┌─────────────────┐
                    │ DEBT EXECUTOR   │ ← Extracts parameters from conversation
                    │   (Python)      │ ← Runs month-by-month calculation
                    └────────┬────────┘ ← Returns JSON with schedule
                             │
                             │ ToolMessage(JSON: {monthly_rows: [...], total_interest: 1850, ...})
                             ▼
                    ┌─────────────────┐
                    │ MEMORY UPDATE   │ ← Saves debt plan to LangGraph Store
                    └────────┬────────┘ ← Namespace: (debt_plans, finance, user_123)
                             │
                             │ State updated, memories saved
                             ▼
                    ┌─────────────────┐
                    │ MAIN ASSISTANT  │ ← NO tools binding (Pass 2)
                    │   (Pass 2)      │ ← Parses JSON from executor
                    └────────┬────────┘ ← Formats as markdown table
                             │
                             ▼
              ┌──────────────────────────────┐
              │  "Here's your 8-month plan:  │
              │  ✅ Debt cleared in 7 months │
              │  💰 Total interest: $1,850   │
              │  [Markdown table with rows]  │
              └──────────────────────────────┘
```

---

## 🔧 CORE COMPONENTS (What Each Node Does)

```
╔═══════════════════════════════════════════════════════════════╗
║  1. MAIN ASSISTANT (Pass 1) - Intent Classification          ║
╠═══════════════════════════════════════════════════════════════╣
║  • Loads user memories from LangGraph Store                   ║
║  • Formats system prompt with role + memories                 ║
║  • Binds RouteIntent tool to LLM                              ║
║  • LLM classifies user intent                                 ║
║  • Returns: AIMessage with tool_calls                         ║
╚═══════════════════════════════════════════════════════════════╝

╔═══════════════════════════════════════════════════════════════╗
║  2. ROUTER - Intent Dispatcher                                ║
╠═══════════════════════════════════════════════════════════════╣
║  • Extracts RouteIntent tool call                             ║
║  • Reads intent field: "debt_payoff_plan"                     ║
║  • Queries Intent Registry                                    ║
║  • Returns: Executor node name                                ║
╚═══════════════════════════════════════════════════════════════╝

╔═══════════════════════════════════════════════════════════════╗
║  3. EXECUTOR - Domain Logic (7 executors available)          ║
╠═══════════════════════════════════════════════════════════════╣
║  • Binds Pydantic schema as LLM tool                          ║
║  • Extracts structured data from conversation                 ║
║  • Runs calculations (Python, NOT LLM)                        ║
║  • Returns: ToolMessage with JSON result                      ║
║                                                               ║
║  Available Executors:                                         ║
║  ├─ finance_debt_payoff_executor (★ Complex calculations)    ║
║  ├─ finance_add_transaction_executor                          ║
║  ├─ finance_budget_executor                                   ║
║  ├─ finance_goal_executor                                     ║
║  ├─ finance_recurring_executor                                ║
║  ├─ finance_summary_executor                                  ║
║  └─ finance_advice_executor                                   ║
╚═══════════════════════════════════════════════════════════════╝

╔═══════════════════════════════════════════════════════════════╗
║  4. MEMORY UPDATE - Persistence Layer                         ║
╠═══════════════════════════════════════════════════════════════╣
║  • Identifies which memories to update (based on intent)      ║
║  • Uses Trustcall to extract structured data                  ║
║  • Saves to LangGraph Store with namespace:                   ║
║    (memory_type, assistant_type, user_id)                     ║
║  • Example: ("finance_debt_plans", "finance", "user_123")     ║
╚═══════════════════════════════════════════════════════════════╝

╔═══════════════════════════════════════════════════════════════╗
║  5. MAIN ASSISTANT (Pass 2) - Response Formatting            ║
╠═══════════════════════════════════════════════════════════════╣
║  • NO tool binding (formatting mode only)                     ║
║  • Parses JSON from executor ToolMessage                      ║
║  • Formats as markdown table                                  ║
║  • Returns: Final AIMessage to user                           ║
║  • CRITICAL: Never recalculates numbers                       ║
╚═══════════════════════════════════════════════════════════════╝
```

---

## 🗂️ DATA LAYER

```
┌─────────────────────────────────────────────────────────────────┐
│  LANGGRAPH STORE (Memory Persistence)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Namespace Structure: (memory_type, assistant_type, user_id)   │
│                                                                 │
│  Examples:                                                      │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ ("profile", "finance", "user_123")                       │  │
│  │   → {name: "Alex", job: "Engineer", location: "SF"}      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ ("finance_transactions", "finance", "user_123")          │  │
│  │   → [{amount: 50, category: "food", date: "2026-02-16"}] │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ ("finance_debt_plans", "finance", "user_123")            │  │
│  │   → {monthly_rows: [...], total_interest: 1850, ...}     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## ⚙️ CONFIGURATION LAYER

```
┌─────────────────────────────────────────────────────────────────┐
│  CONFIGURATION SCHEMA (Universal Config-Driven Design)         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Configuration:                                                 │
│    • user_id: "user_123"                                        │
│    • assistant_type: "finance"                                  │
│    • role_prompt: "You are a Personal Finance Assistant..."     │
│    • enabled_memory_types: [                                    │
│        "profile",                                               │
│        "finance_transactions",                                  │
│        "finance_budgets",                                       │
│        "finance_goals",                                         │
│        "finance_recurring_payments",                            │
│        "finance_debt_plans"                                     │
│      ]                                                          │
│    • router_intents: [                                          │
│        "debt_payoff_plan",                                      │
│        "add_transaction",                                       │
│        "set_budget",                                            │
│        "create_goal",                                           │
│        "add_recurring_payment",                                 │
│        "monthly_summary",                                       │
│        "advice",                                                │
│        "other"                                                  │
│      ]                                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  INTENT REGISTRY (Routing Table)                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  "finance" → {                                                  │
│    "debt_payoff_plan" → "finance_debt_payoff_executor"         │
│    "add_transaction" → "finance_add_transaction_executor"      │
│    "set_budget" → "finance_budget_executor"                    │
│    "create_goal" → "finance_goal_executor"                     │
│    ...                                                          │
│  }                                                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  SCHEMA REGISTRY (Memory Type → Pydantic Schema)                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ("finance", "profile") → Profile                               │
│  ("finance", "finance_budgets") → FinanceBudget                 │
│  ("finance", "finance_debt_plans") → FinanceDebtPlan            │
│  ("finance", "finance_transactions") → FinanceTransaction       │
│  ...                                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 TWO-PASS ARCHITECTURE (Why It Works)

```
┌─────────────────────────────────────────────────────────────────┐
│  PASS 1: Intent Routing                                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  main_assistant:                                                │
│    ✅ Binds RouteIntent tool                                    │
│    ✅ LLM classifies user intent                                │
│    ✅ Returns tool_calls (NOT final response)                   │
│                                                                 │
│  Flow: main_assistant → router → executor → memory_update      │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  PASS 2: Response Formatting                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  main_assistant:                                                │
│    ❌ NO tool binding                                           │
│    ✅ Parses JSON from executor                                 │
│    ✅ Formats as markdown/tables                                │
│    ✅ Returns final response to user                            │
│                                                                 │
│  CRITICAL RULE:                                                 │
│  ⚠️  LLM NEVER recalculates numbers                             │
│  ⚠️  LLM ONLY formats pre-calculated results                    │
│                                                                 │
│  Flow: memory_update → main_assistant → END                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 KEY ARCHITECTURAL PRINCIPLES

```
┌─────────────────────────────────────────────────────────────────┐
│  1. SEPARATION OF CONCERNS                                      │
├─────────────────────────────────────────────────────────────────┤
│  • Configuration defines behavior (not code)                    │
│  • Router classifies intent (no business logic)                 │
│  • Executors handle domain logic (isolated modules)             │
│  • Memory layer persists data (automatic extraction)            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  2. LLM vs PYTHON SEPARATION                                    │
├─────────────────────────────────────────────────────────────────┤
│  LLM (Groq API):                                                │
│    • Intent classification                                      │
│    • Data extraction from conversation                          │
│    • Natural language formatting                                │
│                                                                 │
│  Python:                                                        │
│    • ALL numerical calculations                                 │
│    • Interest compounding                                       │
│    • Month-by-month schedules                                   │
│    • Aggregations and summaries                                 │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  3. EXTENSIBILITY                                               │
├─────────────────────────────────────────────────────────────────┤
│  Add new executor:                                              │
│    1. Create executor file in nodes/executors/                  │
│    2. Create Pydantic schema in schemas/                        │
│    3. Register in intent_registry.py                            │
│    4. Register in schema_registry.py                            │
│    5. Add node to graph.py                                      │
│                                                                 │
│  NO CHANGES NEEDED to:                                          │
│    • main_assistant.py                                          │
│    • router.py                                                  │
│    • memory_update.py                                           │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  4. TYPE SAFETY                                                 │
├─────────────────────────────────────────────────────────────────┤
│  • Pydantic schemas enforce data structure                      │
│  • Schema Registry maps memory types to schemas                 │
│  • Trustcall ensures reliable extraction                        │
│  • All data validated before storage                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🚀 QUICK REFERENCE: File → Responsibility

```
┌────────────────────────────────┬──────────────────────────────────┐
│ FILE                           │ RESPONSIBILITY                   │
├────────────────────────────────┼──────────────────────────────────┤
│ graph.py                       │ Builds state graph               │
│ configuration.py               │ Defines universal config schema  │
│ nodes/main_assistant.py        │ Handles conversation (2 passes)  │
│ nodes/router.py                │ Routes to executors              │
│ nodes/memory_update.py         │ Saves memories to store          │
│ nodes/executors/*.py           │ Domain-specific logic            │
│ schemas/*.py                   │ Pydantic data models             │
│ utils/intent_registry.py       │ Intent → Executor mapping        │
│ utils/schema_registry.py       │ Memory type → Schema mapping     │
│ utils/store_utils.py           │ Store helper functions           │
│ safe_llm.py                    │ Groq API wrapper                 │
└────────────────────────────────┴──────────────────────────────────┘
```

---

## 💡 WHY THIS ARCHITECTURE MATTERS

✅ **Scalable**: Add new domains with config changes only  
✅ **Maintainable**: Each executor is isolated and testable  
✅ **Reliable**: Python handles calculations, not LLM hallucinations  
✅ **Type-Safe**: Pydantic schemas validate all data  
✅ **Observable**: LangSmith tracing for debugging  
✅ **Production-Ready**: Error handling, retries, logging  

---

## 📚 LEARN MORE

- **Full README**: Complete setup guide + usage examples
- **Function Docs**: CSV table with every function documented
 